// prevents obj_controller from selecting more than 1 obj at any one time
global.free_range = true; 

// the instance that is being interacting with
global.locked_instance = noone;

// the object depth when it is being grabbed
global.smallest_depth = -1; 
